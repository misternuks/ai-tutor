<div class="container p-3">
  <div class="card p-3 m-3">
    <%= simple_form_for [@lesson, @chatroom] do |f| %>
      <%= f.input :name %>
      <%= f.button :submit, class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
